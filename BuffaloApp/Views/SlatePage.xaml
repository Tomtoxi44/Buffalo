<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BuffaloApp.ViewModels"
             x:Class="BuffaloApp.Views.SlatePage"
             x:DataType="vm:SlateViewModel"
             Title="Ardoise"
             BackgroundColor="#1a1a2e">

    <RefreshView IsRefreshing="{Binding IsLoading}"
                 Command="{Binding RefreshCommand}">
        <ScrollView>
            <VerticalStackLayout Padding="20" Spacing="20">
                
                <!-- Résumé -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="15">
                    <Frame BackgroundColor="#16213e" CornerRadius="15" Padding="20" BorderColor="Transparent">
                        <VerticalStackLayout HorizontalOptions="Center">
                            <Label Text="📝" FontSize="32" HorizontalOptions="Center"/>
                            <Label Text="{Binding TotalOwedToYou}" 
                                   FontSize="36" 
                                   FontAttributes="Bold"
                                   TextColor="#4ecca3"
                                   HorizontalOptions="Center"/>
                            <Label Text="À récupérer" 
                                   FontSize="12" 
                                   TextColor="#8892b0"
                                   HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </Frame>
                    
                    <Frame Grid.Column="1" BackgroundColor="#16213e" CornerRadius="15" Padding="20" BorderColor="Transparent">
                        <VerticalStackLayout HorizontalOptions="Center">
                            <Label Text="💸" FontSize="32" HorizontalOptions="Center"/>
                            <Label Text="{Binding TotalOwed}" 
                                   FontSize="36" 
                                   FontAttributes="Bold"
                                   TextColor="#e94560"
                                   HorizontalOptions="Center"/>
                            <Label Text="À payer" 
                                   FontSize="12" 
                                   TextColor="#8892b0"
                                   HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </Frame>
                </Grid>

                <!-- Ardoises qu'on me doit -->
                <Frame BackgroundColor="#16213e" 
                       CornerRadius="15" 
                       Padding="20"
                       BorderColor="Transparent">
                    <VerticalStackLayout Spacing="15">
                        <Label Text="🎯 À récupérer" 
                               FontSize="18" 
                               FontAttributes="Bold"
                               TextColor="#4ecca3"/>
                        <Label Text="Ces joueurs te doivent un Buffalo !"
                               FontSize="12"
                               TextColor="#8892b0"/>

                        <CollectionView ItemsSource="{Binding SlatesOwedToYou}">
                            <CollectionView.EmptyView>
                                <Label Text="Personne ne te doit de Buffalo"
                                       FontSize="14"
                                       TextColor="#8892b0"
                                       HorizontalOptions="Center"
                                       Margin="0,20"/>
                            </CollectionView.EmptyView>
                            
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="vm:SlateDisplayItem">
                                    <Frame BackgroundColor="#0f3460" 
                                           CornerRadius="10" 
                                           Padding="15"
                                           Margin="0,5"
                                           BorderColor="Transparent">
                                        <Grid ColumnDefinitions="*,Auto">
                                            <VerticalStackLayout>
                                                <Label Text="{Binding OtherPlayerName}" 
                                                       FontSize="16" 
                                                       FontAttributes="Bold"
                                                       TextColor="White"/>
                                                <Label Text="{Binding DateText}" 
                                                       FontSize="12" 
                                                       TextColor="#8892b0"/>
                                                <Label Text="{Binding LocationText}" 
                                                       FontSize="12" 
                                                       TextColor="#8892b0"/>
                                            </VerticalStackLayout>
                                            
                                            <Button Grid.Column="1"
                                                    Text="✓ Réglé"
                                                    BackgroundColor="#4ecca3"
                                                    TextColor="#1a1a2e"
                                                    CornerRadius="8"
                                                    Padding="15,8"
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SlateViewModel}}, Path=SettleSlateCommand}"
                                                    CommandParameter="{Binding .}"/>
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Frame>

                <!-- Ardoises que je dois -->
                <Frame BackgroundColor="#16213e" 
                       CornerRadius="15" 
                       Padding="20"
                       BorderColor="Transparent">
                    <VerticalStackLayout Spacing="15">
                        <Label Text="⏳ À payer" 
                               FontSize="18" 
                               FontAttributes="Bold"
                               TextColor="#e94560"/>
                        <Label Text="Ces joueurs peuvent te redonner ton Buffalo à tout moment !"
                               FontSize="12"
                               TextColor="#8892b0"/>

                        <CollectionView ItemsSource="{Binding SlatesOwed}">
                            <CollectionView.EmptyView>
                                <Label Text="Tu ne dois aucun Buffalo"
                                       FontSize="14"
                                       TextColor="#8892b0"
                                       HorizontalOptions="Center"
                                       Margin="0,20"/>
                            </CollectionView.EmptyView>
                            
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="vm:SlateDisplayItem">
                                    <Frame BackgroundColor="#0f3460" 
                                           CornerRadius="10" 
                                           Padding="15"
                                           Margin="0,5"
                                           BorderColor="Transparent">
                                        <Grid ColumnDefinitions="Auto,*">
                                            <Label Text="⚠️" FontSize="24" VerticalOptions="Center"/>
                                            <VerticalStackLayout Grid.Column="1" Margin="10,0,0,0">
                                                <Label Text="{Binding OtherPlayerName}" 
                                                       FontSize="16" 
                                                       FontAttributes="Bold"
                                                       TextColor="White"/>
                                                <Label Text="{Binding DateText}" 
                                                       FontSize="12" 
                                                       TextColor="#8892b0"/>
                                                <Label Text="{Binding LocationText}" 
                                                       FontSize="12" 
                                                       TextColor="#8892b0"/>
                                            </VerticalStackLayout>
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>
        </ScrollView>
    </RefreshView>
</ContentPage>
